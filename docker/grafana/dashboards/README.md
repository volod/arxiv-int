# Provisioned Dashboards

Repository-owned Grafana dashboards are added here by the discovery-visualization capability.
Grafana mounts this directory read-only; mutable UI state belongs under `SERVICE_STATE_DIR`.
